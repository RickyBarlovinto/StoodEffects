<resources>
    <!-- (Existing styles: CircularButton, InfoCard, etc. are kept) -->

    <!-- Front Page Title Styles -->
    <style name="MainTitle" parent="Widget.AppCompat.TextView">
        <item name="android:textSize">34sp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textColor">#4A00E0</item>
        <item name="android:layout_marginBottom">24dp</item>
        <item name="android:gravity">center</item>
    </style>

    <!-- Modern Buttons -->
    <style name="ModernButton" parent="Widget.MaterialComponents.Button.UnelevatedButton">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:paddingStart">24dp</item>
        <item name="android:paddingEnd">24dp</item>
        <item name="android:paddingTop">12dp</item>
        <item name="android:paddingBottom">12dp</item>
        <item name="android:textColor">@android:color/white</item>
        <item name="android:textSize">16sp</item>
        <item name="android:textStyle">bold</item>
        <item name="cornerRadius">50dp</item>
    </style>

    <style name="ModernButton.Start">
        <item name="android:backgroundTint">#8A2BE2</item> <!-- Solid color fallback for gradient -->
    </style>

    <style name="ModernButton.History">
        <item name="android:backgroundTint">#E9ECEF</item>
        <item name="android:textColor">#495057</item>
    </style>

    <style name="ModernButton.Danger" parent="ModernButton">
        <item name="android:backgroundTint">#F44336</item>
    </style>

</resources>
